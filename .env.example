# Copy this file to .env.modal and replace the placeholder values.
# Never commit .env.modal or any real token.
# Read access is enough for training; uploading a model requires write access
# to the destination Hugging Face repository.
HF_TOKEN=

# Add future runtime secrets here when needed, for example:
# WANDB_API_KEY=
